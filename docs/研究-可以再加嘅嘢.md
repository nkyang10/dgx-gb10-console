# 研究 — 仲有咩可以叫我研究 / collect（候選菜單）

> 目的：而家個收集範圍（狀態-01..08）以外，仲有咩**值得加**。每項標：攞啲乜／價值／成本／屬「研究定直接 collect」。
> 來源：綜合一輪常識 + Serper 搜尋（LLM serving observability / GPU 功耗成本 / token 會計 / 節流史 / fabric benchmark）。

---

## A. 成本 / 能量 / 容量規劃（企業最鍾意問）

| # | 項目 | 攞啲乜 | 價值 | 成本 | 研究定 collect |
|---|---|---|---|---|---|
| A1 | **能量/電費追蹤** | power rails × time → **kWh/日**、電費（$）、idle 成本%、總功耗 vs 上限 | 高（慳錢/provision） | 中 | 🟢 唔使研究，直接加（spark_hwmon energy accumulator × interval） |
| A2 | **Token 用量/會計** | 每 model **per-day tokens**、`$ / 1M tokens` 成本、cache-hit 慳咗幾多、預算爆 alert | 高 | 中 | 🟢 直接加（Sparky cumulative token tracker pattern；cost 要你畀率價） |
| A3 | **容量預測** | disk **「幾時滿」**（linear fit TTF）、KV headroom 趨勢、GPU 記憶體 headroom →「要唔要加 node」 | 高 | 低 | 🔵 要少少研究（線性外推方法） |

## B. 更深推理觀察（vLLM 層）

| # | 項目 | 攞啲乜 | 價值 | 成本 | 研究定 collect |
|---|---|---|---|---|---|
| B1 | **Per-request latency tracing** | OTel/Jaeger spans：prefill/decode 各 phase 分佈、逐 request 追蹤 | 中高 | 高 | 🔵 要研究（OTel exporter + traces 整合） |
| B2 | **Live per-request tok/s + session** | 每個正在跑 request 嘅即時速度 | 中 | 中 | 🟡 可選（Sparky 有現成做法） |
| B3 | **Model-level SLO / per-model 分開** | 每個 served model 獨立 performance + SLO goodput + backlog | 中高 | 中 | 🔵 要研究（`model_name` label 分組） |
| B4 | **Engine sleep / KV-block 深** | `engine_sleep_state` 轉變史、KV block lifetime/idle（long-context 尾耗記憶體） | 中 | 低 | 🔵 少少研究（新 metric 名已經有） |
| B5 | **Ray cluster 健康**（若跨節點） | Ray dashboard :8265, NCCL collective health, cluster bus timeout | 中 | 中 | 🔵 要研究（只適用 Ray 形態） |

## C. 主動 / 基準測試（active probing，唔係被動睇）

| # | 項目 | 攞啲乜 | 價值 | 成本 | 研究定 collect |
|---|---|---|---|---|---|
| C1 | **Fabric bandwidth 實測** | perftest `ib_write_bw` / `ib_read_lat` → 實測 200G vs 廣告（跨節點） | 高（確認 fabric 真效能） | 中 | 🔵 要研究（幾時跑唔好騷擾 inference） |
| C2 | **Memory bandwidth baseline** | 273GB/s 實測對照 → 睇退化 | 中 | 中 | 🔵 要研究 |
| C3 | **Tiny inference latency probe** | 真發一個極短 request 度 **real TTFT**（唔靠 histogram） | 高 | 低 | 🟡 少少研究（要唔好洗亂 production） |
| C4 | **GPU throttle 主動檢查** | spark-gpu-throttle-check（cuBLAS load）→ 主動 detect stuck clock | 中高 | 中 | 🔵 要研究（CUDA load 可能打擾 vLLM，要小心） |

## D. 健康 / 可靠性

| # | 項目 | 攞啲乜 | 價值 | 成本 | 研究定 collect |
|---|---|---|---|---|---|
| D1 | **NVMe 壽命預測** | write amplification、**days-remaining**（wear rate）、temperature 史 | 中高 | 低 | 🔵 少少研究（推公式） |
| D2 | **Container OOM/restart** | per-container restart count、OOM kill、`docker inspect` 異常史 | 中 | 低 | 🟢 直接加（docker inspect RestartCount/OOMKilled） |
| D3 | **Kernel/OOM log forensics** | journald `/var/log/kern.log` OOM/throttle/NVMe error 事件 | 中（出事時高） | 中 | 🔵 要研究（Loki-lite 或 tail 解析） |
| D4 | **風扇/EC/節流史** | 風扇 RPM、EC firmware、節流事件時間線（幾時 throttle 咗幾耐） | 中 | 低 | 🔵 要研究（EC 有**用 chhard 得知，純 OS 未必） |

## E. 網絡 / 集群深

| # | 項目 | 攞啲乜 | 價值 | 成本 | 研究定 collect |
|---|---|---|---|---|---|
| E1 | **RDMA 擁塞** | CNP/ECN rate、重傳率、link error 趨勢 | 中高 | 低 | 🔵 少少研究（counter 已有，要算 rate） |
| E2 | **NVLink/PCIe util + 拓撲** | 逐 link 頻寬 util、topology map | 中 | 中 | 🔵 要研究（GB10 冇 NVLink 對外？要確認） |

## F. 安全

| # | 項目 | 攞啲乜 | 價值 | 成本 | 研究定 collect |
|---|---|---|---|---|---|
| F1 | **Security posture** | open ports/listening services、failed SSH login、異常 uptime、`last` | 中 | 低 | 🟢 直接加 |
| F2 | **Setting 監察** | systemd failed、docker 異常、非預期 process | 低中 | 低 | 🟢 直接加 |

---

## 🎯 我最建議優先研究嘅（若你想）

1. **A3 容量預測**（disk TTF + KV headroom）→ 好實用、成本低
2. **A1 能量/成本**（kWh + 電費）→ 幾乎零研究，直接加
3. **C3 tiny TTFT probe** → 真正量度 real 延遲（好多 status API 漏咗）
4. **A2 Token 會計/cost** → 如果你想要成本 views
5. **D2 container OOM/restart** → 出事時最有用，直接加

> 如果你嘅「performance」意思仲想含**主動 benchmark**（C1–C4）／**per-model 分開**（B3）／**tracing**（B1），話我知。

## G. vLLM 隊列 / 控制（新加）

| # | 項目 | 攞啲乜 | 價值 | 成本 | 研究定 collect |
|---|---|---|---|---|---|
| G1 | **vLLM job queue 觀看** | ✅ 已完成（R7）：無 per-request listing（靠 proxy /v1/responses 追）；有 /load dict、/server_info、is_paused/is_sleeping（dev） | 🔵 R7 完成 |
| G2 | **vLLM on-the-fly 管理可能性** | ✅ 已完成（R7 verified v0.28.0）：控制全 dev-gate；安全 = abort-by-id / pause(keep\|wait) / resume；⚠️ GB10 sleep-wake crash #50011 | 🔵 R7 完成 → 開放決定 OD-4 |

---

## 下一步

你揀想研究／加邊啲（可多選），我就照揀咗嗰啲落 `SPEC.md` 做新 assumption（ASM-15..）+ 補 research。儲存/API 呈現方式（OD-1）仍然係**你話我知**先落 code。
